[網頁版貪食蛇遊戲] PRD
一、專案概述
專案名稱： 網頁版貪食蛇遊戲
一句話描述： 一個基於瀏覽器的 2D 貪食蛇遊戲，具備響應式設計、觸控支援及本地最高分紀錄功能，旨在為用戶提供流暢且經典的休閒體驗。
版本： v1.0.0
二、問題與目標
問題陳述
用戶在瀏覽器中尋找簡單、無需安裝且能快速開始的休閒遊戲時，往往遇到操作複雜、視覺過時或在移動端無法操作（無觸控支援）的問題。

目標
提供一個視覺現代化（暗色霓虹風格）的貪食蛇遊戲。
實現多端適配，支援鍵盤（方向鍵/WASD）與觸控（滑動）操作。
確保遊戲性能流暢，並具備即時分數與狀態反饋。
三、目標用戶
用戶特徵
喜愛經典街機風格遊戲的玩家。
需要在工作或學習空檔進行短時間休閒的上班族/學生。
使用手機或平板瀏覽網頁的用戶。
用戶需求
即時可玩： 打開網頁即可遊玩，無需註冊或下載。
操作直覺： 桌面滑鼠/鍵盤操作，移動端觸控滑動操作。
挑戰性： 隨著遊戲進行，速度應適當提升以增加難度。
四、用戶故事
用戶故事 1： 作為一名玩家，我想要使用鍵盤方向鍵或 WASD 控制蛇的移動，以便我習慣性地進行遊玩。
用戶故事 2： 作為一名手機用戶，我想要透過在畫面上滑動手指來改變方向，以便在沒有鍵盤的設備上也能操作。
用戶故事 3： 作為一名玩家，我想要系統自動保存我的最高分，即使刷新頁面也能看到我之前的最佳成績。
用戶故事 4： 作為一名玩家，我希望能夠暫停遊玩並稍後繼續，以便處理突發的現實事務。
五、功能需求與驗收標準
功能 1：核心遊戲機制
描述： 蛇在網格上移動，吃掉紅色蘋果增長身體與分數；撞牆或撞到自己則遊戲結束。每吃一個蘋果，速度會稍微加快。
驗收標準（Given/When/Then）：
Given 遊戲正在運行中
When 蛇頭座標與蘋果座標重合
Then 分數增加 10 分，蛇身長度增加 1，並在隨機空位生成新蘋果
Given 蛇正在移動
When 蛇頭撞擊邊界 或身體任何部位
Then 遊戲迴圈停止，顯示「遊戲結束」彈窗與最終得分
Given 遊戲進行中
When 玩家獲得分數
Then 遊戲速度（間隔時間）應適當遞減，直到達到最小間隔 (60ms)
功能 2：控制系統
描述： 支援鍵盤輸入與觸控手勢輸入，並防止非法的 180 度轉向（例如：向右時不能直接向左）。
驗收標準：
Given 遊戲處於進行中狀態
When 用戶按下與當前方向相反的方向鍵（如當前向右，按下左）
Then 蛇的方向保持不變（忽略該次輸入）
Given 用戶使用觸控裝置
When 用戶在 Canvas 上滑動手指
Then 系統根據滑動的 X/Y 軸位移差判斷方向並控制蛇
功能 3：遊戲狀態管理
描述： 遊戲需具備「開始」、「暫停」、「進行中」、「結束」四種狀態，並透過 UI 按鈕與鍵盤（空白鍵）進行切換。
驗收標準：
Given 遊戲未開始或已暫停
When 點擊「開始遊戲」或按下空白鍵
Then 計時器啟動，按鈕文字變為「遊戲進行中」或「繼續遊戲」
Given 遊戲進行中
When 點擊「暫停遊戲」或按下空白鍵
Then 計時器清除，畫面靜止，按鈕變為「繼續遊戲」
功能 4：數據持久化與介面顯示
描述： 使用 localStorage 存儲最高分，並即時更新介面上的分數、長度與最高分。
驗收標準：
Given 頁面首次加載
When 讀取 localStorage 中的 snakeHighScore
Then 介面「最高分」區塊正確顯示該數值
Given 當前得分超過歷史最高分
When 更新分數時
Then 最高分數值自動更新，並同步寫入 localStorage
六、技術約束
必須遵守
使用原生 HTML5 <canvas> 進行渲染。
所有代碼（HTML, CSS, JS）必須包含在同一個檔案中便於部署。
遊戲邏輯必須與渲染分離。
不允許使用 alert()，必須使用自定義 DOM 元素（如 #gameOverScreen）顯示訊息。
兼容性要求
支援 Chrome, Firefox, Safari, Edge 等現代瀏覽器。
響應式設計：在螢幕寬度小於 650px 時，Canvas 寬度需調整為 100%，且控制按鈕需垂直排列。
不要做
不要引入外部圖片資源（蘋果、蛇等圖形需由 Canvas API 繪製）。
不要引入第三方遊戲引擎（如 Phaser, Unity），保持輕量化。
七、現有代碼