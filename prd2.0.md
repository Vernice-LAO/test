[網頁版貪食蛇遊戲 (含虛擬方向鍵)] PRD
一、專案概述
專案名稱： 跨平台響應式貪食蛇遊戲
一句話描述： 一個基於 HTML5 Canvas 的貪食蛇遊戲，整合了鍵盤控制與螢幕虛擬方向鍵（上、下、左、右），提供桌面與手機一致的流暢體驗。
版本： v2.0.0 (新增強制顯示虛擬按鍵功能)
二、問題與目標
問題陳述
早期的網頁版貪食蛇遊戲通常只支援鍵盤操作，導致手機用戶無法遊玩；或者透過滑動手勢控制，容易發生誤判導致遊戲體驗不佳。此外，若按鈕僅在隱藏模式下顯示，開發者或測試人員在桌面端瀏覽器無法驗證該功能的可用性。

目標
全平台控制： 實現一套同時支援鍵盤（方向鍵/WASD）、滑鼠點擊與觸控（Touch）的雙重控制機制。
可視化操作介面： 在頁面底部永久顯示四個方向按鍵，解決手機操作精準度問題，並方便桌面端測試。
遊戲性強化： 隨著分數增加提升遊戲速度，並保存歷史最高分數以增加重玩價值。
三、目標用戶
用戶特徵
使用手機瀏覽器進行碎片化娛樂的用戶。
習慣使用實體鍵盤操作的桌面玩家。
需要一個簡單、乾淨、無廣告的休閒遊戲的用戶。
用戶需求
即時反饋： 按下按鈕時要有視覺反饋（下沉、變色），確認操作已被接收。
操作便利： 按鈕尺寸需適合手指點擊，且佈局需符合直覺（十字方向鍵）。
狀態清晰： 能清楚看到當前分數、蛇長度以及歷史最高分。
四、用戶故事
用戶故事1： 作為一名手機用戶，我想要在螢幕下方看到具體的「上、下、左、右」按鈕，這樣我就能精準地控制蛇的轉向，而不依賴容易誤觸的滑動手勢。
用戶故事2： 作為一名桌面用戶，我也想看到這些按鈕，以便在沒有鍵盤的情況下（如平板模式）也能使用滑鼠點擊進行遊玩。
用戶故事3： 作為一名玩家，我想要系統自動保存我的最高分，這樣即使關閉瀏覽器後再回來，我依然能看到自己的最佳成績並嘗試打破它。
用戶故事4： 作為一名玩家，當我按下方向按鈕時，按鈕應該有明顯的按壓動畫，讓我知道指令已經送出。
五、功能需求與驗收標準
功能1：虛擬方向鍵
描述： 在遊戲畫布下方顯示四個圓形按鈕，分別對應上、下、左、右。
驗收標準（Given/When/Then）：
Given 使用者進入遊戲頁面
When 查看頁面底部
Then 必須能看到四個排列成十字形的按鈕（▲、◀、▼、▶），且在寬螢幕（桌面）下也顯示，不可隱藏。
Given 使用者點擊「上」按鈕
When 按鈕被按下
Then 蛇必須向上轉向（若當前非向下），且按鈕樣式改變（變色/下沉）提供視覺回饋。
功能2：雙重輸入支援
描述： 同時監聽鍵盤事件 與 滑鼠/觸控事件。
驗收標準：
Given 遊戲進行中
When 使用者按下鍵盤 W 鍵 或 點擊螢幕「上」按鈕
Then 兩者皆應觸發相同的「向上移動」邏輯。
功能3：遊戲速度動態調整
描述： 隨著吃到的蘋果數量增加，遊戲刷新頻率加快。
驗收標準：
Given 初始遊戲速度為 120ms
When 蛇吃掉一個蘋果
Then 遊戲速度應減少 2ms（變快），直到達到最小值 60ms 為止。
六、技術約束
必須遵守
遊戲核心使用 <canvas> 2D Context 繪製。
所有代碼（HTML, CSS, JS）必須封裝在單一文件中。
禁止使用 alert()，需使用 DOM 元素（如 #gameOverScreen）顯示訊息。
使用 localStorage 進行數據存儲。
兼容性要求
支援 iOS Safari (需處理 touchstart 的 preventDefault 防止縮放)。
支援 Android Chrome。
支援 Chrome, Firefox, Edge 等桌面瀏覽器。
不要做
不要依賴外部圖片資源，所有圖形需由程式碼繪製。
不要引入 jQuery, React 等第三方庫，保持輕量化。
七、現有代碼